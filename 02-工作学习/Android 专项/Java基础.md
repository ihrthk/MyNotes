# Java基础

[同程面试：说一下多态的实现原理？ - 掘金](Java%E5%9F%BA%E7%A1%80/%E5%90%8C%E7%A8%8B%E9%9D%A2%E8%AF%95%EF%BC%9A%E8%AF%B4%E4%B8%80%E4%B8%8B%E5%A4%9A%E6%80%81%E7%9A%84%E5%AE%9E%E7%8E%B0%E5%8E%9F%E7%90%86%EF%BC%9F%20-%20%E6%8E%98%E9%87%91%2073d008087f6548c89c9e9b2fe54e4bda.md)

[Android 最常讨论的关于 Kotlin 的面试问题](Java%E5%9F%BA%E7%A1%80/Android%20%E6%9C%80%E5%B8%B8%E8%AE%A8%E8%AE%BA%E7%9A%84%E5%85%B3%E4%BA%8E%20Kotlin%20%E7%9A%84%E9%9D%A2%E8%AF%95%E9%97%AE%E9%A2%98%200e9f8e36e17d40d582f28ea4881cf104.md)

- HashMap 1.7
    - 无参构造
        - capacity = 16
        - loadFactor = 0.75
        - transient Entry<K,V>[] table
        - Entry
            - key
            - value
            - next
            - hash
    - put方法
        - inflateTable
            - roundUpToPowerOf2
                - number > 1
                - Integer.highestOneBit((number - 1) << 1)
                    - i |= (i >> 1);
                    - i |= (i >> 2);
                    - i |= (i >> 4);
                    - i |= (i >> 8);
                    - i |= (i >> 16);
                    - i - (i >>> 1);
                - 1
            - threshold
            - table
        - putForNullKey
            - 替换null的旧值
            - modCount++
            - addEntry
        - 二次hash
            - 扰动函数
            - h ^= (h >>> 20) ^ (h >>> 12);
            - h ^ (h >>> 7) ^ (h >>> 4);
        - indexFor → h & (table.length - 1)
            - 1001 0011
            - 0000 1111
            - 删除h前面的0
            - 高效运算
        - 替换相同key的旧值
        - modCount++
        - addEntry
            - 判断扩容的条件
                - size >= threshold
                - null != table[bucketIndex]
                - resize(2*table.length)
                    - Entry[] newTable = new Entry[newCapacity];
                    - transfer → for (Entry<K,V> e : table) → while(null != e)
                        - 保存链表下一个 Entry<K,V> next = e.next;
                        - int i = indexFor(e.hash, newCapacity);
                        - e.next = newTable[i];
                        - newTable[i] = e;
                        - e = next;
    - get方法
        - getKey == null → getForNullKey
        - getEntry
            - h = hash(key)
            - indexFor(h,table.length)
            - table[i]
            - e.hash == hash && key.equals(k)
            
    - 面试问题
        - 数据结构是怎么样？数组+链表
        - 怎么样插入数据？头插法
        - 哈希冲突怎么预防和解决？二次哈希、拉链法
        - 默认的容量？16
        - 数组是怎么创建的？第一次put的时候
- HashMap 1.8
    - 无参的构造方法
        - capacity = 16
        - loadFactor = 0.75
    - put方法
        - (h = key.hashCode()) ^ (h >>> 16)
        - putVal(int hash, K key, V value, boolean onlyIfAbsent,boolean evict)
            - onlyIfAbsent → true的话，有值就不添加了。
            - evict → LinkedHashMap用的，达到上限，则驱逐最老的元素
            - table == null || table.length == 0 → resize
                - 确定newCap&newThr
                    - int oldCap = (oldTab == null) ? 0 : oldTab.length;
                    - oldCap > 0
                    - oldThr > 0
                    - newCap = DEFAULT_INITIAL_CAPACITY;
                - Node<K,V>[] newTab = (Node<K,V>[])new Node[newCap];
                
            - (p = tab[i = (n - 1) & hash]) == null
                - tab[i] = newNode(hash, key, value, null);
                - existNode → Node<K,V> e; K k;
            - ++modCount
            - ++size > threshold → resize